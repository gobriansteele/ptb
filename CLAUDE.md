# Claude Code Instructions

## Interaction Style

- Always use the `AskUserQuestion` tool when interviewing the user or gathering requirements. Never output interview questions as plain text.

## Git Workflow

- Always create a feature branch before writing any code. Never commit directly to `main`.
- Branch naming convention: `feature/<short-description>`
